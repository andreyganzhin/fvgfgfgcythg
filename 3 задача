#include <bits/stdc++.h>
using namespace std;

int main() {
    int x;
    int d = 2;
    cin >> x;
    while(d*d <= x && x % d != 0){ 
        d++;
    }
    if(x % d != 0){
        cout << x;
    }
    else{
        cout << d;
    }
        
    
            
        
    return 0;
    


}
